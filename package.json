{
  "name": "vifu",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "npm-packages/*",
    "packages/*"
  ],
  "scripts": {
    "check": "bun run check:boundaries && bun run check:public && bun run typecheck",
    "check:boundaries": "node scripts/check-dashboard-boundaries.mjs",
    "check:public": "node scripts/check-public-repo.mjs",
    "typecheck": "bun run --cwd packages/protocol typecheck && bun run --cwd packages/runtime typecheck && bun run --cwd packages/sdk typecheck && bun run --cwd npm-packages/dashboard typecheck",
    "build:packages": "bun run --cwd packages/protocol build && bun run --cwd packages/runtime build && bun run --cwd packages/sdk build",
    "build": "bun run build:packages && bun run --cwd npm-packages/dashboard build",
    "test": "bun run build:packages && vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "vitest": "^3.2.4"
  }
}
